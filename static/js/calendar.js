// Simple date-time picker initialization (if needed)
window.onload = function() {
    var datetime = document.getElementById('time');
    if(datetime) {
        datetime.type = 'datetime-local';
    }
}
